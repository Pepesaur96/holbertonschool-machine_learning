#!/usr/bin/env python3
"""Module for the function np_transpose(matrix)."""


def np_transpose(matrix):
    """
    Function that transposes a 2D matrix.
    Args:
        - matrix: a list of lists whose transpose should be returned
    Returns:
        A new list of lists representing the transpose of matrix
    """
    return matrix.transpose()
